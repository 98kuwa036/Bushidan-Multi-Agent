# Bushidan Multi-Agent System v9.1 - Dependencies
# Universal Multi-LLM Framework - Simplified and Optimized

# Core dependencies (required)
httpx>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0

# AI Model APIs (required)
anthropic>=0.3.0              # Claude Sonnet 4.5 for Shogun
google-generativeai>=0.3.0    # Gemini 2.0 Flash for Karo
litellm>=1.0.0               # OpenAI-compatible API for Qwen

# Web search and scraping (optional but recommended)
tavily-python>=0.3.0          # Smart web search
playwright>=1.40.0            # Content extraction

# Optional integrations
slack-sdk>=3.21.0             # Slack Bot interface
notion-client>=2.0.0          # Long-term memory (Notion)

# Development dependencies (dev only)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.5.0

# System utilities
pathlib2>=2.3.7

# Installation notes:
# 
# Minimal (core functionality):
#   pip install anthropic google-generativeai litellm httpx python-dotenv pydantic
#
# Recommended (with web search):
#   pip install -r requirements.txt
#   playwright install chromium
#
# Full development setup:
#   pip install -r requirements.txt
#   playwright install chromium
#   pip install -e .[dev]
#
# Note: v9.1 eliminates heavy dependencies from v8.1:
#   - No Docker (was 6.1.0+)
#   - No Qdrant (was 1.7.0+) 
#   - No SQLAlchemy (was 2.0.0+)
#   - No PyTorch (was 2.0.0+)
#   - No FastAPI (was 0.115.0+)
#   
# This reduces installation size by ~2GB and complexity by 80%