# Bushidan Multi-Agent System v14 - Dependencies
# 10役職体制 + LangGraph v14 + MCP SDK + モバイルコンソール

# Core dependencies
httpx>=0.24.0
aiohttp>=3.8.0
psutil>=5.9.0                 # システム情報 (CPU/メモリ/ディスク)
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0
asyncio-throttle>=1.0.2

# AI Model APIs (v14 - 10役職)
anthropic>=0.40.0             # Claude Opus 4.6 (大元帥) + Sonnet 4.6 (将軍)
openai>=1.50.0                # o3-mini (軍師)
google-genai>=1.0.0           # Gemini API (新SDK: google-genai, httpx経由で使用)
groq>=0.4.0                   # Llama 3.3 70B (斥候)
cerebras-cloud-sdk>=1.0.0    # gpt-oss-120b (高速筆耕 Stage 2)
cohere>=5.0.0                 # Command R (受付) + Command R+ (外事)
mistralai>=1.0.0              # Mistral Large 3 (参謀)

# LangGraph (v14 routing)
langgraph>=1.0.0
langchain-core>=0.3.0
langchain-mcp-adapters>=0.1.0
langgraph-checkpoint-postgres>=2.0.0  # PostgresSaver (Phase 1)
psycopg[binary]>=3.1.0               # PostgreSQL driver (psycopg3)

# Web search
tavily-python>=0.3.0
playwright>=1.40.0

# Integrations
discord.py>=2.3.0
notion-client>=2.0.0
mattermostdriver>=7.3.2
mcp>=1.0.0

# v14: Mobile Console
fastapi>=0.110.0
uvicorn>=0.29.0
bcrypt>=4.0.0                 # Web UI パスワードハッシュ

# Data analysis
pandas>=1.3.0
tabulate>=0.8.9

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.5.0
