# Bushidan Multi-Agent System v9.4 - Hybrid Architecture Dependencies
# BDI Framework + llama.cpp CPU Optimized + 3-Tier Fallback

# Core dependencies (required)
httpx>=0.24.0
aiohttp>=3.8.0               # Enhanced async HTTP for web search
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0
asyncio-throttle>=1.0.2      # Rate limiting for API calls

# AI Model APIs (required for 4-tier architecture with intelligent routing)
anthropic>=0.34.0             # Claude Sonnet 4.5 + Opus 4 (Shogun) - Prompt Caching
google-generativeai>=0.4.0    # Gemini 3.0 Flash (Final defense)
groq>=0.4.0                   # Groq Llama 3.3 70B (Simple tasks, 300-500 tok/s)
litellm>=1.0.0               # OpenAI-compatible API proxy
dashscope>=1.0.0             # Alibaba Cloud Qwen3-Coder-Plus (Kagemusha)

# DSPy integration (recommended)
dspy>=2.0.0                  # Prompt optimization and translation layer

# Enhanced web search and scraping
tavily-python>=0.3.0          # Precise URL discovery
playwright>=1.40.0            # Targeted content extraction

# Optional integrations
slack-sdk>=3.21.0             # Slack Bot interface
notion-client>=2.0.0          # Long-term memory (Notion)

# Development dependencies (dev only)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.5.0

# System utilities
pathlib2>=2.3.7

# ============================================================================
# v9.4 Installation Guide
# ============================================================================
#
# Minimal (Cloud-only, no local inference):
#   pip install anthropic google-generativeai groq dashscope httpx python-dotenv pydantic
#
# Recommended (Full hybrid with intelligent routing):
#   pip install -r requirements.txt
#   playwright install chromium
#
# Full development setup:
#   pip install -r requirements.txt
#   playwright install chromium
#   pip install -e .[dev]
#
# ============================================================================
# v9.4 Local Qwen3-Coder Setup (llama.cpp CPU Optimized)
# ============================================================================
#
# Option 1: Automatic setup (HP ProDesk 600)
#   chmod +x scripts/setup_llamacpp_prodesck600.sh
#   ./scripts/setup_llamacpp_prodesck600.sh
#
# Option 2: Manual setup
#   # Build llama.cpp
#   git clone https://github.com/ggerganov/llama.cpp.git
#   cd llama.cpp && mkdir build && cd build
#   cmake .. -DCMAKE_BUILD_TYPE=Release -DLLAMA_NATIVE=ON -DLLAMA_AVX2=ON
#   make -j$(nproc)
#
#   # Download model (~17GB)
#   mkdir -p models
#   wget https://huggingface.co/Qwen/Qwen3-Coder-30B-A3B-Instruct-GGUF/resolve/main/qwen3-coder-30b-a3b-instruct-q4_k_m.gguf
#
#   # Start server
#   ./llama-server -m models/qwen3-coder-30b-a3b-instruct-q4_k_m.gguf \
#       -c 4096 -t 8 -b 512 --mlock --host 127.0.0.1 --port 8080
#
# See: docs/LLAMACPP_SETUP.md for full documentation
#
# ============================================================================
# v9.4 Architecture
# ============================================================================
#
#   ✅ BDI Framework (Belief-Desire-Intention formal reasoning)
#   ✅ llama.cpp CPU optimized (HP ProDesk 600 ready)
#   ✅ Intelligent routing (運用黄金律)
#   ✅ Groq for simple tasks (free, 300-500 tok/s)
#   ✅ Local Qwen3 (llama.cpp, 4k context, ¥0)
#   ✅ Cloud Qwen3-plus (Kagemusha, 32k context)
#   ✅ Gemini 3.0 Flash (final defense)
#   ✅ Claude Prompt Caching (90% savings)
#   ✅ Japanese logging throughout
#
# Expected Performance (HP ProDesk 600 i7-10700):
#   - Simple tasks: 2s (Groq)
#   - Medium tasks: 15s (Local Qwen3 CPU, 15-25 tok/s)
#   - 3-tier fallback: 99.5% reliability
#
# Storage Requirements:
#   - 本陣 (CT 100): 10GB (orchestration only)
#   - Qwen3 (CT 101): 45GB (llama.cpp + 17GB model)
